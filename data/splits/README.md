# Experimental split manifests

No split manifests have been uploaded. This directory is reserved for the actual device, date, observation-record and enrollment/validation/test assignments used in the experiments.

Any future manifest should identify the dataset, evaluation setting, run or split, observation record and assigned subset. Open-set manifests should also identify the known and held-out device classes. No assignments have been inferred from aggregate table values.
